def suma(a, b):                    #palabra reservada return, 
    resultado = a + b              # a+b se define variable como resultado
    return resultado                # le pedimos qeu nos edvuelva el resultado


c = suma (2, 6)                   #   c = se define variable como c
d = suma (c, 2)                   # D = se define variable y le pedimos que a C se sume 
print(d)                          # que nos imprima d